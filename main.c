/* Arquivo antigo convertido em nota.
   A implementação modular foi movida para `src/` e cabeçalhos em `include/`.
   Use `make` para compilar e execute `./build/game`.

   Este arquivo permanece apenas por referência; o código fonte principal
   agora está em `src/` conforme a estrutura requerida pelo enunciado.
*/

